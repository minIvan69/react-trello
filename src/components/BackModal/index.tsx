import React, { FunctionComponent } from "react";
import { BackgroundModalStyles } from "./styles";

const BackgroundModal: FunctionComponent = ({ children }) => {
  return <BackgroundModal>{children}</BackgroundModal>;
};

export default BackgroundModal;
